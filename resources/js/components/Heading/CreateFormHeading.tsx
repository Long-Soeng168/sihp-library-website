const CreateFormHeading = ({ title }: { title: string }) => {
    return (
        <>
            <h2 className="mb-6 text-xl font-semibold text-primary">{title}</h2>
        </>
    );
};

export default CreateFormHeading;
